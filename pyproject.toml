[project]
name = "workspace-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["cowsay"]

[tool.uv.workspace]
members = ["packages/*"]
