[project]
name = "pkg-a"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["cowsay", "lib"]

[tool.uv.sources]
lib = { workspace = true }

[project.scripts]
a = "pkg_a:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"
